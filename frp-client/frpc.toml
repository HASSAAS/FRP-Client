serverAddr = "your_server_addr"
serverPort = 7000
auth.method = "token"
auth.token = "123456789"

# Keep wire protocol v1 for compatibility with existing FRP servers.
transport.wireProtocol = "v1"
transport.tls.enable = true

log.to = "console"
# trace, debug, info, warn, error
log.level = "info"
log.maxDays = 3

# transport.tls.certFile = "/share/frp/fullchain.pem"
# transport.tls.keyFile = "/share/frp/privkey.pem"
# transport.tls.trustedCaFile = "/share/frp/fullchain.pem"
# transport.tls.serverName = "your_server_name"

webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "123456789"

[[proxies]]
name = "your_proxy_name"
type = "http"
customDomains = ["your_domain"]
transport.useEncryption = true
transport.useCompression = true
localPort = 8123
localIP = "127.0.0.1"

# HTTPS alternative for a local HTTP Home Assistant service.
# Remove or comment out the HTTP proxy above before enabling this block.
# [[proxies]]
# name = "your_https_proxy_name"
# type = "https"
# customDomains = ["your_domain"]
# transport.useEncryption = true
# transport.useCompression = true
#
# [proxies.plugin]
# type = "https2http"
# localAddr = "127.0.0.1:8123"
# crtPath = "/ssl/fullchain.pem"
# keyPath = "/ssl/privkey.pem"
# hostHeaderRewrite = "127.0.0.1"
# requestHeaders.set.x-forwarded-proto = "https"
